package com.alotra.controller;

// Disabled: This class duplicated ProductController's add-to-cart mapping (POST /products/{id}/add-to-cart)
// Keep as a placeholder without @Controller so Spring MVC does not register it.
// If you need custom behavior later, merge into ProductController instead of reintroducing duplicate routes.

public class ProductCartController {
    // ... intentionally blank ...
}